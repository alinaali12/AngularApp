import { PagePermissionDirective } from './page-permission.directive';

describe('PagePermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new PagePermissionDirective();
    expect(directive).toBeTruthy();
  });
});
