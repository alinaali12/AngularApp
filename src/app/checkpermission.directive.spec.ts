import { CheckpermissionDirective } from './checkpermission.directive';

describe('CheckpermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckpermissionDirective();
    expect(directive).toBeTruthy();
  });
});
