import { StudentRegisterationModel } from './student-registeration-model';

describe('StudentRegisterationModel', () => {
  it('should create an instance', () => {
    expect(new StudentRegisterationModel()).toBeTruthy();
  });
});
