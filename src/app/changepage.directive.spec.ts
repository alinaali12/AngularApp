import { ChangepageDirective } from './changepage.directive';

describe('ChangepageDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangepageDirective();
    expect(directive).toBeTruthy();
  });
});
